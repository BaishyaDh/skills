{
  "name": "skills",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/skills",
  "projectType": "library",
  "targets": {
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "git add . && git commit -m \"chore: update skills\" || true && git push origin master",
        "cwd": "packages/skills"
      }
    }
  }
}
